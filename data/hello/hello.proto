syntax = "proto3";
package hello;

message hello {
    // if isinit is set, listen will ret another hello
    bool isinit = 1;
    // ws path
    string entry = 2;
    uint64 mask = 3;
    // host name
    string name = 4;
    string myhost = 5;
}
